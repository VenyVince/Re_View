<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.review.shop.repository.Orders.OrderMapper">

    <select id="getOrderCheckoutProductInfo" resultType="com.review.shop.dto.orders.OrderCheckoutProductInfoDTO">
        SELECT
            P.PRODUCT_ID   AS prdId,
            (SELECT IMAGE_URL
             FROM PRODUCT_IMAGE
             WHERE PRODUCT_ID = P.PRODUCT_ID
               AND ROWNUM = 1) AS prdImage,
            P.PRD_NAME     AS prdName,
            P.PRICE        AS prdPrice,
            #{quantity}    AS buy_quantity
        FROM
            PRODUCT P
        WHERE
            P.PRODUCT_ID = #{prdId}
    </select>

    <select id="getUserPoint" resultType="integer">
        SELECT
            POINT
        FROM
            USER_TABLE  WHERE
            USER_ID = #{userId}
    </select>

</mapper>
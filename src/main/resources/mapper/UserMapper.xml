<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.review.shop.repository.user.UserMapper">
    <!-- user id는 Oracle 트리거로 자동 생성, created date는 db에서 default sys date로 지정됨. -->
    <insert id="insertUser" parameterType="UserInfoDto">
        INSERT INTO USER_TABLE (id, password, name, email, nickname, phone_number, baumann_id, role)
        VALUES (#{id}, #{password}, #{name}, #{email}, #{nickname}, #{phoneNumber}, #{baumannId}, #{role})
    </insert>
<!--    UserInfoDto findUserById(String id)에 대한 sql문-->
    <select id="findUserById" resultType="com.review.shop.dto.user.UserInfoDto">
        SELECT
            ID,PASSWORD,NAME,EMAIL,NICKNAME,PHONE_NUMBER,BAUMANN_ID,ROLE
        FROM
            USER_TABLE WHERE
            ID = #{id}
    </select>
</mapper>



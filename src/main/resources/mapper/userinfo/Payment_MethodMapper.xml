<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.review.shop.repository.userinfo.other.Payment_MethodMapper">

    <!-- 결제 수단 조회 -->
    <select id="getPayments" resultType="com.review.shop.dto.userinfo.others.Payment_MethodDTO">
        SELECT * FROM payment_methods
        WHERE user_id = #{user_id}
    </select>

    <!-- 결제 수단 존재 여부 -->
    <select id="existsPayment" resultType="int">
        SELECT CASE WHEN COUNT(*) > 0 THEN 1 ELSE 0 END
        FROM payment_methods
        WHERE user_id = #{user_id}
          AND card_number = #{card_number}
    </select>

    <!-- 결제 수단 추가 -->
    <insert id="addPayment">
        INSERT INTO payment_methods (user_id, card_company, card_number)
        VALUES (#{user_id}, #{card_company}, #{card_number})
    </insert>

    <!-- 기본 결제 수단 설정 -->
    <update id="checkDefaultPayment">
        UPDATE payment_methods
        SET is_default = CASE
                             WHEN payment_id = #{payment_id} THEN 1
                             ELSE 0
            END
        WHERE user_id = #{user_id}
    </update>

    <!-- 결제 수단 삭제 -->
    <delete id="deletePayments">
        DELETE FROM payment_methods
        WHERE user_id = #{user_id}
          AND payment_id = #{payment_id}
    </delete>

</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.review.shop.repository.admin.AdminReviewMapper">
    <update id="deleteReview" parameterType="int">
        UPDATE REVIEW
        SET DELETED_AT = SYSDATE
        WHERE REVIEW_ID = #{review_id}
    </update>

    <update id="setReviewSelection">
        UPDATE REVIEW
        SET IS_SELECTED = #{is_selected}
        WHERE REVIEW_ID = #{review_id}
    </update>

    <select id="findReviewer" resultType="int">
        select user_id
        from review
        where review_id = #{review_id}
    </select>

    <select id="getAllReviews" resultType="com.review.shop.dto.review.ReviewAdminDTO">
        SELECT
            r.review_id,
            COALESCE(u.nickname, u.name) AS writer,
            r.content,
            p.price,
            r.is_selected
        FROM review r
                 JOIN USER_table u ON r.user_id = u.user_id
                 JOIN product p ON r.product_id = p.product_id
        WHERE r.deleted_at IS NULL
    </select>

</mapper>
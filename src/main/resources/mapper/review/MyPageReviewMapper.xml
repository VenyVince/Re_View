<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--sort: 생성일, 별점, 추천-->
<mapper namespace="com.review.shop.repository.search.MyPageProductPage.MyPageReviewMapper">

    <resultMap id="myPageReviewMap" type="com.review.shop.dto.search.MyPageProductPage.MyPage.MyPageReviewDTO">
        <id property="review_id" column="review_id"/>

        <result property="prd_name" column="prd_name"/>
        <result property="content" column="content"/>
        <result property="created_at" column="created_at"/>
        <result property="rating" column="rating"/>
        <result property="baumann_id" column="baumann_id"/>

        <collection property="image_urls" ofType="java.lang.String">
            <result column="image_url"/>
        </collection>
    </resultMap>

    <select id="SearchMyReviews" resultMap="myPageReviewMap">
        SELECT
        p.prd_name,
        r.content, r.created_at, r.rating,
        u.baumann_id,
        ri.image_url
        FROM review r
        LEFT JOIN review_images ri ON r.review_id = ri.review_id
        JOIN product p ON r.product_id = p.product_id
        JOIN user_table u on r.user_id = u.user_id
        WHERE r.user_id = #{user_id}
        and r.deleted_at is null
        and p.deleted_at is null
        <if test="keyword != null and keyword != ''">
            AND (
            p.prd_name LIKE '%' || #{keyword} || '%'
            OR r.content LIKE '%' || #{keyword} || '%'
            )
        </if>

        <if test="filter_rating > 0">
            AND r.rating &gt;= #{filter_rating}
        </if>

        <choose>
            <when test="sort == 'latest'">
                ORDER BY r.created_at DESC
            </when>
            <when test="sort == 'oldest'">
                ORDER BY r.created_at ASC
            </when>
            <when test="sort == 'rating_highest'">
                ORDER BY r.rating DESC
            </when>
            <when test="sort == 'rating_lowest'">
                ORDER BY r.rating ASC
            </when>
            <when test="sort == 'likes'">
                ORDER BY r.like_count DESC
            </when>
            <otherwise>
                ORDER BY r.created_at DESC
            </otherwise>
        </choose>
    </select>

</mapper>

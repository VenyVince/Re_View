<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.review.shop.repository.product.ProductMapper">

    <select id="selectProductList" resultType="com.review.shop.dto.product.ProductDTO">
        SELECT
        product_id,
        prd_name,
        prd_brand,
        'https://your-image-url.com/images/' || product_id as image_url,
        price,
        category,
        rating
        FROM product
        WHERE deleted_at IS NULL
        AND is_sold_out = '0'

        <if test="category != null and category != ''">
            AND category = #{category}
        </if>

        <trim prefix="ORDER BY">
            <choose>
                <when test="sort == 'rating'">
                    rating DESC, product_id DESC
                </when>
                <when test="sort == 'price'">
                    price ASC, product_id DESC
                </when>
                <otherwise>
                    product_id DESC
                </otherwise>
            </choose>
        </trim>

        OFFSET #{offset} ROWS FETCH NEXT #{size} ROWS ONLY
    </select>

    <select id="selectProductCount" resultType="int">
        SELECT COUNT(*)
        FROM product
        WHERE deleted_at IS NULL
        AND is_sold_out = '0'
    </select>

    <select id="selectProductDetail" resultType="com.review.shop.dto.product.ProductDetailDTO">
        SELECT
        product_id,
        prd_name,
        prd_brand,
        price,
        category,
        rating,
        ingredient,
        description,
        stock,
        review_count,
        is_sold_out,
        baumann_id
        FROM product
        WHERE product_id = #{product_id}
        AND deleted_at IS NULL
    </select>

    <select id="selectProductImages" resultType="string">
        SELECT image_url
        FROM product_image
        WHERE product_id = #{product_id}
    </select>


</mapper>